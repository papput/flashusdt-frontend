import { HomePage } from "@/components/pages/HomePage";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "FLASH USDT PRO",
  description:
    "Experience the next generation of cryptocurrency transactions with the FLASH USDT PRO interface.",
};

export default function Page() {
  return <HomePage />;
}
